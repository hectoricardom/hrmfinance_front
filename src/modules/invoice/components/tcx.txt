
      {/* Shipper/Consignee Information */}
      <div style={sectionStyle}>
        <h3 style={sectionHeaderStyle}>
          <span>🚚</span>
          <span>Información del Remitente/Destinatario</span>
        </h3>
        
       
        <div style={fullWidthGridStyle}>
          <SearchableCustomerDropdown
            value={props.customer}
            onChange={props.onCustomerChange}
            placeholder="Buscar cliente por nombre, teléfono, ID..."
            label="Cliente"
            description="Busque y seleccione un cliente existente o ingrese los datos manualmente"
          />
        </div>


        {/* Manual shipper input */}
        <div style={gridStyle}>
          <div>
            <label style={labelStyle}>Nombre *</label>
            <input
              type="text"
              style={inputStyle}
              value={props.customer.name}
              onInput={(e) => props.onShipperConsigneeChange({ 
                name: (e.target as HTMLInputElement).value 
              })}
              placeholder="Nombre completo"
              required
            />
          </div>
          
          <div>
            <label style={labelStyle}>Primer Nombre *</label>
            <input
              type="text"
              style={inputStyle}
              value={props.customer.firstName || ''}
              onInput={(e) => {
                const firstName  = (e.target as HTMLInputElement).value;
                if (props.customer) {
                  props.onCustomerChange({ ...props.customer, firstName  });
                } else {
                  props.onCustomerChange({
                    id: '',
                    name: '',
                    firstName,
                    idS: '',
                    dob: '',
                    addressS: ''
                  });
                }
              }}
              placeholder="Primer nombre"
              required
            />
          </div>
          
          <div>
            <label style={labelStyle}>Segundo Nombre</label>
            <input
              type="text"
              style={inputStyle}
              value={props.customer.middleName || ''}
              onInput={(e) => {
                const firstName  = (e.target as HTMLInputElement).value;
                if (props.customer) {
                  props.onCustomerChange({ ...props.customer, firstName  });
                } else {
                  props.onCustomerChange({
                    id: '',
                    name: '',
                    firstName,
                    idS: '',
                    dob: '',
                    addressS: ''
                  });
                }
              }}
              placeholder="Segundo nombre (opcional)"
            />
          </div>
          
          <div>
            <label style={labelStyle}>Primer Apellido *</label>
            <input
              type="text"
              style={inputStyle}
              value={props.customer.lastName || ''}
              onInput={(e) => {
                const firstName  = (e.target as HTMLInputElement).value;
                if (props.customer) {
                  props.onCustomerChange({ ...props.customer, firstName  });
                } else {
                  props.onCustomerChange({
                    id: '',
                    name: '',
                    firstName,
                    idS: '',
                    dob: '',
                    addressS: ''
                  });
                }
              }}
              placeholder="Primer apellido"
              required
            />
          </div>
          
          <div>
            <label style={labelStyle}>Segundo Apellido</label>
            <input
              type="text"
              style={inputStyle}
              value={props.customer.lastName2 || ''}
              onInput={(e) => {
                const firstName  = (e.target as HTMLInputElement).value;
                if (props.customer) {
                  props.onCustomerChange({ ...props.customer, firstName  });
                } else {
                  props.onCustomerChange({
                    id: '',
                    name: '',
                    firstName,
                    idS: '',
                    dob: '',
                    addressS: ''
                  });
                }
              }}
              placeholder="Segundo apellido (opcional)"
            />
          </div>
        </div>

        <div style={twoColumnGridStyle}>
          <div>
            <label style={labelStyle}>Cédula/ID *</label>
            <input
              type="text"
              style={inputStyle}
              value={props.customer.cid}
              onInput={(e) => {
                const firstName  = (e.target as HTMLInputElement).value;
                if (props.customer) {
                  props.onCustomerChange({ ...props.customer, firstName  });
                } else {
                  props.onCustomerChange({
                    id: '',
                    name: '',
                    firstName,
                    idS: '',
                    dob: '',
                    addressS: ''
                  });
                }
              }}
              placeholder="Número de cédula o ID"
              required
            />
          </div>
          
          <div>
            <label style={labelStyle}>Pasaporte</label>
            <input
              type="text"
              style={inputStyle}
              value={props.customer.passport || ''}
              onInput={(e) => {
                const firstName  = (e.target as HTMLInputElement).value;
                if (props.customer) {
                  props.onCustomerChange({ ...props.customer, firstName  });
                } else {
                  props.onCustomerChange({
                    id: '',
                    name: '',
                    firstName,
                    idS: '',
                    dob: '',
                    addressS: ''
                  });
                }
              }}
              placeholder="Número de pasaporte (opcional)"
            />
          </div>
        </div>

        <div style={twoColumnGridStyle}>
          <div>
            <label style={labelStyle}>Fecha de Nacimiento</label>
            <input
              type="date"
              style={inputStyle}
              value={props.customer.dob || ''}
              onInput={(e) => {
                const firstName  = (e.target as HTMLInputElement).value;
                if (props.customer) {
                  props.onCustomerChange({ ...props.customer, firstName  });
                } else {
                  props.onCustomerChange({
                    id: '',
                    name: '',
                    firstName,
                    idS: '',
                    dob: '',
                    addressS: ''
                  });
                }
              }}
            />
          </div>
          
          <div>
            <label style={labelStyle}>Email</label>
            <input
              type="email"
              style={inputStyle}
              value={props.customer.email || ''}
              onInput={(e) => {
                const firstName  = (e.target as HTMLInputElement).value;
                if (props.customer) {
                  props.onCustomerChange({ ...props.customer, firstName  });
                } else {
                  props.onCustomerChange({
                    id: '',
                    name: '',
                    firstName,
                    idS: '',
                    dob: '',
                    addressS: ''
                  });
                }
              }}
              placeholder="Correo electrónico"
            />
          </div>
        </div>

        <div style={twoColumnGridStyle}>
          <div>
            <label style={labelStyle}>Teléfono Principal *</label>
            <input
              type="tel"
              style={inputStyle}
              value={props.customer.phoneNumber || ""}
              onInput={(e) => {
                const firstName  = (e.target as HTMLInputElement).value;
                if (props.customer) {
                  props.onCustomerChange({ ...props.customer, firstName  });
                } else {
                  props.onCustomerChange({
                    id: '',
                    name: '',
                    firstName,
                    idS: '',
                    dob: '',
                    addressS: ''
                  });
                }
              }}
              placeholder="Teléfono principal"
              required
            />
          </div>
          <div>
            <label style={labelStyle}>Teléfono</label>
            <input
              type="tel"
              style={inputStyle}
              value={props.customer?.phoneNumberS || ''}
              onInput={(e) => {
                const phoneNumber = (e.target as HTMLInputElement).value;
                if (props.customer) {
                  props.onCustomerChange({ ...props.customer, phoneNumber });
                } else {
                  props.onCustomerChange({
                    id: '',
                    name: '',
                    phoneNumberS: phoneNumber,
                    idS: '',
                    dob: '',
                    addressS: ''
                  });
                }
              }}
              placeholder="Número de teléfono"
            />
          </div>
          
          <div>
            <label style={labelStyle}>Teléfono Secundario</label>
            <input
              type="tel"
              style={inputStyle}
              value={props.customer.phoneNumber || ''}
              onInput={(e) => {
                const firstName  = (e.target as HTMLInputElement).value;
                if (props.customer) {
                  props.onCustomerChange({ ...props.customer, firstName  });
                } else {
                  props.onCustomerChange({
                    id: '',
                    name: '',
                    firstName,
                    idS: '',
                    dob: '',
                    addressS: ''
                  });
                }
              }}
              placeholder="Teléfono secundario (opcional)"
            />
          </div>
        </div>

        <div style={fullWidthGridStyle}>
          <div>
            <label style={labelStyle}>Dirección</label>
            <input
              type="text"
              style={inputStyle}
              value={props.customer.address || ''}
              onInput={(e) => {
                const firstName  = (e.target as HTMLInputElement).value;
                if (props.customer) {
                  props.onCustomerChange({ ...props.customer, firstName  });
                } else {
                  props.onCustomerChange({
                    id: '',
                    name: '',
                    firstName,
                    idS: '',
                    dob: '',
                    addressS: ''
                  });
                }
              }}
              placeholder="Dirección completa"
            />
          </div>
        </div>
      </div>
    </>